{
  "name": "my_google_analytics",
  "version": "1.0.0",
  "description": "vingle 6th tech talk hands on labs",
  "main": "./src/handler.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "serverless deploy",
    "deploy:prod": "npm run deploy -- -s prod"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "serverless": "^1.30.1"
  },
  "dependencies": {
    "aws-sdk": "^2.302.0"
  }
}
